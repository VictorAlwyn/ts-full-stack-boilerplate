// Cross-platform authentication utilities only
export {
  authUtils,
  tokenStorage,
  userStorage,
  setStorageAdapter,
  browserStorageAdapter,
  type StorageAdapter,
} from "./client";

// QueryClient for React Query
export { queryClient } from "./client";
